from typing import TYPE_CHECKING
if TYPE_CHECKING:
    from pytpp.api.websdk.models import (
        certificate, client, codesign, config, config_schema, credential, discovery, flow, hsm_api, identity,
        log, metadata, oauth, permissions, pki, preferences, processing_engines, recycle_bin, secret_store,
        ssh, ssh_certificates, stats, system_status, workflow
    )
